# TropiGo Environment Configuration
# Copy this file to .env and update with your actual values
# NEVER commit .env file to version control

# Database Configuration
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=tropigo

# API Configuration
GOOGLE_GENERATIVE_AI_API_KEY=your_google_ai_api_key_here

# UI Configuration
# For Docker Compose with nginx proxy: Use /api (relative path - nginx proxies to backend)
# For local development without proxy: Use http://localhost:8060/api
# For production with custom domain: Update VITE_API_URL to your production API URL
VITE_API_URL=/api

# Production Notes:
# - Use strong passwords for POSTGRES_PASSWORD in production
# - Get your Google AI API key from: https://makersuite.google.com/app/apikey
# - For production deployment: Update VITE_API_URL to your production API URL if needed
